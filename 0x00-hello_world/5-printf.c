#include <stdio.h>

int main(void)
{
        /*
         * main function to print using printf
         *
         * return 0 for success
         */
        printf("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
